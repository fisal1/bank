import React from 'react';
import ReactDOM from 'react-dom/client';
import BankIntelApp from './BankIntelApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BankIntelApp />
  </React.StrictMode>
);
